<section class="copy" id="{{ $route ?? 'generic' }}-copy">
    <div class="copy-inner">
        <h1>
            {{ $page->mantle_headline }}
        </h1>

        {!! $page->mantle_text !!}
    </div>
</section>
